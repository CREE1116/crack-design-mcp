---
name: crack-story-design
description: Design, compress, and audit Crack story-chat projects. Write cohesive 7,000-char prompts via the 4-Step Squeeze Loop, manage 3-slot keyword books via JSON, enforce the 4-part output contract, and test with isolated personas.
---

# Crack Story Design Skill

이 스킬은 AI 모델이 **풍부하고 매력적인 메인 서사를 유기적인 하나의 완성본으로 자유롭게 통작성**하고, 복잡한 기계적 작업(글자 수 실시간 측정, 마크다운 포맷팅, 키워드북 3슬롯 관리, 압축 알림, 페르소나 격리 테스트)은 **`crack-design-mcp` 도구**에 위임하는 전문 창작 워크플로우를 안내합니다.

---

## 1. 역할 분담 원칙

1. **메인 프롬프트는 모델이 "통으로" 작성**:
   - 자잘한 슬롯 채우기 대신, 세계관·캐릭터 명부·출력 계약·씽킹 체크를 결합하여 **하나의 완성된 7,000자 메인 프롬프트 전문**을 모델이 직접 통으로 작성합니다 (`write_main_prompt`).
2. **키워드북은 로직이 기계적으로 포맷팅 & 엔트리**:
   - 마크다운 서식을 모델이 직접 타이핑하지 않고, 모델이 **JSON 포맷**으로 전달하면 도구가 마크다운/DB에 자동 배치합니다 (`batch_import_keywords`, `update_keyword_entry`).
3. **제한 초과 시 차단 없는 "압축 알림 (Soft Cap)"**:
   - 7,000자(메인 프롬프트)나 400자(키워드 항목)를 넘겨도 저장을 거부하지 않고 일단 안전하게 저장합니다.
   - 도구가 초과 글자 수를 명시한 알림(`needs_compression: True`)을 주므로, 모델은 문맥을 살려 점진적으로 다듬고 압축합니다.
4. **리플라이 테스트 시 페르소나 완전 분리**:
   - 전지적 지식 누출을 원천 차단하기 위해, `get_tester_context`로 **순수 무지 플레이어 관점**과 **현재 3개 키워드만 아는 서술자 관점**을 분리하여 에뮬레이션합니다.

---

## 2. 핵심 작성 팁 & 골든 룰 (Golden Rules)

### ① 4단계 반복 압축 루프 (Iterative Squeeze Loop)
> ⚡ **골든 룰**: 처음부터 글자 수 한도를 의식하고 쓰면 생각이 위축되어 앙상하고 뻔한 규칙만 남습니다. 모든 산출물은 한도를 **130~150% 초과하도록 모든 설정과 디테일을 쏟아부은 뒤, 압축해서 우겨넣고, 더 넣고, 다시 우겨넣는 과정**을 거쳐야 극한의 정보 밀도(Maximum Information Density)가 완성됩니다.

- **Step 1 (오버 기획)**: 7,000자 한도를 무시하고 9,000~10,000자 수준으로 인과, 심리, 대사 톤, 오감 디테일을 총망라하여 작성.
- **Step 2 (1차 압축 우겨넣기)**: 문장·조사·중복 주어를 제거하고, 전역 공유 기호(`ⓤ`, `ⓒ`, `→`, `·`, `/`, `｜`, `×`, `↑/↓`) 및 표(Table)로 문법 승격. (여유 공간 확보)
- **Step 3 (추가 정보 밀어넣기)**: ★ 남은 공간에 NPC 티키타카, 엣지 케이스 방어, 세부 경제/물가 수치를 추가 주입하여 다시 초과.
- **Step 4 (2차 극한 재압축)**: 고밀도 전보체와 인라인 약어로 재압축하여 최종 한도의 **95~98%(6,500~6,850자)를 꽉 채워 완성**.

### ② 전역 공유 기호 체계 (Global Shared Syntax)
메인 프롬프트 `# 표기`에서 선언한 기호 체계를 시작 프롬프트, 키워드북, 단축어 등 모든 산출물이 재정의 없이 그대로 공유합니다:
- `ⓤ`: 플레이어 / 유저 (`ⓤ 자율성 6중 잠금`, `ⓤ 행동 존중`)
- `ⓒ`: NPC / 상대 캐릭터 (`ⓒ 선제적 개입 적극 장려`)
- `→`: 인과 및 전환 (`IF(조건)→결과`, `A→B(인과)`)
- `·`: 병렬 및 동시 (`대사·행동·생각 대리서술 금지`)
- `/`: 선택 및 대안 (`남/여`, `슬롯 열거`)
- `｜`: 속성 구분 파이프 (`▶인물:[성별｜직급｜나이｜종족｜성격...]`)
- `×`: 금지선 및 차단 (`대리서술×`, `재인용×`, `임의가공×`)
- `↑ / ↓`: 수치 증감 (`호감도↑`, `의심↓`)
- `⌛`: 턴/응답 번호 (`[⌛1]`, `[⌛n]`)

### ③ 유저 자율성 6중 잠금 & 안티 에코 (Anti-Echo)
- **6중 잠금**: ⓤ의 대사·행동·생각·감정·선택 대리서술 절대금지. 1/2/3 번호 선택지 및 "어떻게 할 것인가?"식 유도 질문문 절대 금지.
- **안티 에코 (Anti-Echo)**: 지문 내 ⓤ 대사 재인용("...") 및 `{{user}} | "..."` 대사 블록 출력 절대금지. AI 출력은 오직 ⓒ 대사와 나레이션만 존재.
- **ⓒ의 능동적 개입**: ⓤ의 결정권은 보장하되, 주변 NPC들이 질문, 제안, 티키타카를 먼저 던져 서사가 멈추지 않도록 드라이브.

### ④ 3단 입력 파싱 규약
- **1순위 `[ ]`**: 메타 지시문 최우선 적용.
- **2순위 `*text*`**: 설정/지문 입력으로 파싱 (대사가 아니며, 반박 없이 100% 수용).
- **3순위 평문**: 인캐릭터 롤플레이 대사/행동으로 처리.
- **/OOC**: 롤플레이 즉시 정지, 상태창 생략, 순수 해설 모드 전환.

### ⑤ 3슬롯 키워드북 토폴로지 운용법
- **상시 등장인물 단독 키워드화 절대 금지**: `라임`, `모나` 같은 주연 이름을 단독 키워드로 잡으면 대화마다 매 턴 3슬롯을 상시 독점(100% 발동)하여 정작 필요한 사건/아이템 지식이 밀려납니다.
  - 상시 인물의 기본 말투/외형은 **메인 프롬프트 인물 명부**에 두고, 키워드북에는 `라임 비밀`, `모나 과거`, `니아 폭주` 등 **심층/비밀 복합 트리거**로만 분리합니다.
- **2개 슬롯만 상시 가용, 1개 슬롯은 여유 버퍼 확보**: 평상시 0~2개만 활성화되도록 키워드를 엄격히 좁혀야, 유저의 돌발 질문 시 3번째 슬롯에 해당 정보가 안전하게 들어옵니다.
- **구어체/파생어 등록**: "사랑의 묘약 성분표 뭐야?"에 대응하도록 `["사랑의 묘약", "묘약 성분", "미약"]` 등 자연스러운 채팅 구어체를 함께 포함합니다.

### ⑥ 프롤로그 7단계 줌인 & Hot-start (In media res)
- **7단계 줌인**: 원경(세계관 풍경) ➡️ 중경(장소 공간감) ➡️ 근경(오감 앵커) ➡️ 상황 압력 ➡️ 첫 인물 등장 ➡️ 상호작용 착수 ➡️ Hot-start 바톤 넘김.
- **상투어 및 AI 질문 금지**: "~의 서막이었다", "어떻게 하시겠습니까?"를 절대 쓰지 말고, 인물의 눈빛과 기다리는 동작으로 긴장감 있게 턴을 넘깁니다.

### ⑦ 4부 출력 계약 & [INFO] HUD
- **위치 & 형식**: 지문 나레이션(`*기울임표*`) ➡️ 대사(`이름 | "대사"`) ➡️ 최하단 [INFO] 박스.
- **[INFO] HUD**: 장소, 시간, 날씨, 등장인물, 호감도(`(←변화 근거)` 필수), 상황 1줄 요약.

---

## 3. 실전 5단계 창작 파이프라인

### [Step 1] 메인 프롬프트 통작성
```json
write_main_prompt({
  "content": "# 표기\n- 치환: {user}=ⓤ...\n# 1. 필수 추론...\n...",
  "variant": "safe"
})
```
- 반환값 확인: UTF-16 기준 글자 수 및 `needs_compression` 알림 확인. (목표 6,500~6,850자)

### [Step 2] 프롤로그 및 시작 상황 통작성
```json
write_opening({
  "prologue": "*마왕성 최상층... 라임 대리가 서류를 건넸다.*",
  "opening_situation": "# 첫 턴 입력 처리 규약\n1. ⓤ가 프로필을 입력하면 자연스럽게 호칭에 반영...\n[시작 상황: 첫 번째 결재]\n..."
})
```
- 반환값 확인: 각각 1,000자 이하 검증.

### [Step 3] 키워드북 JSON 일괄 등록
```json
batch_import_keywords({
  "entries": [
    {
      "title": "사랑의 묘약 성분표",
      "keywords": ["사랑의 묘약", "묘약 성분", "미약"],
      "bullets": [
        "기획 1팀 극비 개발품: 마족 호르몬 촉진제 함유",
        "주의: 과다 복용 시 슬라임화 현상 촉발",
        "라임 대리가 캐비닛 3번째 서랍에 시제품 은닉 중"
      ]
    }
  ],
  "variant": "both"
})
```
- 400자 초과 알림 수신 시 내용 압축 후 업데이트.

### [Step 4] 원클릭 종합 감사
```json
audit_project()
```
- 7,000자 / 1,000자 / 400자 한도, 미정의 기호, 3슬롯 충돌 위험 키워드 자동 검출.

### [Step 5] 페르소나 격리 롤플레잉 테스트
1. `start_session({"session": "test_run", "variant": "safe"})`
2. **턴 루프**:
   - `get_tester_context({"session": "test_run", "role": "player"})` ➡️ 비밀을 모르는 순수 신입 관점 입력 생성.
   - `get_tester_context({"session": "test_run", "role": "narrator", "input": ...})` ➡️ 현재 3개 키워드만 아는 서술자 컨텍스트로 응답 생성.
   - `play_turn({"session": "test_run", "input": ..., "reply": ...})` ➡️ 턴 제출 및 findings(출력 계약 위반), dropped(3슬롯 누락) 확인.
3. `activation_report({"session": "test_run"})` ➡️ 키워드북 발동 빈도 및 3슬롯 병목 분석.

---

## 4. 지원 도구 및 가이드 안내

언제든지 필요할 때 다음 도구로 표준 양식과 심층 가이드를 호출할 수 있습니다:
- `get_design_guide({"topic": "writing_tips"})`: 4단계 Squeeze Loop 및 전역 기호 상세 가이드.
- `get_design_guide({"topic": "keyword_rules"})`: 3슬롯 Starvation 방지 및 토폴로지 가이드.
- `get_design_guide({"topic": "opening_design"})`: 7단계 줌인 프롤로그 및 Hot-start 가이드.
- `get_design_guide({"topic": "output_contract"})`: 4부 출력 계약 및 [INFO] HUD 가이드.
- `get_template({"template_name": "main_prompt"})`: 메인 프롬프트 표준 스켈레톤.
- `get_template({"template_name": "character", "name": "라임", "title": "대리", "role": "상품기획"})`: 캐릭터 3분리 스켈레톤.
